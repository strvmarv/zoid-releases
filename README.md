# zoid releases

Prebuilt binaries for [zoid](https://github.com/strvmarv/zoid) — an event-sourced terminal agent.

The source lives in a private repository; this repo hosts the downloadable release binaries only.

See the latest release for platform installers and archives. After installing, `zoid update` self-updates from here.
